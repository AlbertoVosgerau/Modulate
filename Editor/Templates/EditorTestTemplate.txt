using NUnit.Framework;

namespace #Namespace#.Tests.Editor
{  
    // To open Test Runner go to Window -> General -> Test Runner
    public class #Name#EditorTests
    {
        // Some silly test
        [TestCase(7, true)]
        [TestCase(4, true)]
        public void IsPrime_ReturnsExpectedResult(int number, bool expected)
        {
            Assert.AreEqual(expected, IsPrime(number));
        }
        
        public static bool IsPrime(int number)
        {
            if (number < 2) return false;
            if (number == 2) return true;
            if (number % 2 == 0) return false;

            int limit = (int)System.Math.Sqrt(number);
            for (int i = 3; i <= limit; i += 2)
            {
                if (number % i == 0) return false;
            }

            return true;
        }
    }
}