using System;
using DandyDino.Modulate;

namespace #Namespace#
{
    [Serializable]
    public class #Name#Manager : IManager<#Name#View>
    {
        public #Name#View => _view; 
        private #Name#View _view;
        
        private #Name#Manager()
        {
            EventBus<RegisterViewEvt>.OnEvent += (evt) => _view = (#Name#View)evt.view;
            EventBus<UnRegisterViewEvt>.OnEvent += (evt) => _view = null;
        }
    }
}